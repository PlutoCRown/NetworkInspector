{
  "name": "network-inspector",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --filter @network-inspector/extension dev",
    "build": "bun run --filter @network-inspector/extension build",
    "test": "bun test packages/presets/src/parse-template.test.ts && bun run --filter @network-inspector/extension test"
  }
}
