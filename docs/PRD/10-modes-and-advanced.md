# 10 范围说明（当前版本）

当前版本 **仅实现普通模式**：

- 表单化规则组编辑
- 预设 Renderer（card / divider）
- 字段表达式 + 全局 Processor / Alias
- 全量配置导入导出

以下能力 **不在当前范围**，勿在实现中预留兼容：

- 自定义 HTML 模板
- 规则级 `decode` 脚本
- 高级模式 UI 开关

若后续增加，需单独 PRD 修订。
